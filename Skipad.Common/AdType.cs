namespace Inqwise.Skipad.Common
{
    public enum AdType
    {
        Undefined = 0,
        InLine = 1,
        Wrapper = 2,
    }

    public enum SkipType
    {
        Slide = 0,
        Push = 1,
        PushTrueView = 2,
    }
}